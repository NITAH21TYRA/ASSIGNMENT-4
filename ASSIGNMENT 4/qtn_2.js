const person = {
    firstName: "Jane",
    lastName: "Doe"
  };
  
  // Add the new property "country" with the value "Uganda" using dot notation (preferred)
  person.country = "Uganda"
  
  console.log(person)// This will print the updated object with the new property